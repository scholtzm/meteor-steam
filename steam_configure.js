Template.configureLoginServiceDialogForSteam.fields = function () {
  // leaving this array empty makes the confirmation button in config dialog disabled, derp
  return [{property:'dummy', label: 'Anything'}];
};
